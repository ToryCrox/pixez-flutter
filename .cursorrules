# PixEz Flutter 项目 Cursor Rules

## 项目概述

PixEz 是一个使用 Flutter 构建的第三方 Pixiv 客户端，支持 Android、iOS、Windows 和 Linux 平台。项目使用多种状态管理方案，主要基于 MobX，部分使用 Riverpod。

## 技术栈

- **Flutter SDK**: >=3.32.0
- **状态管理**: MobX（主要）、Riverpod（部分页面）
- **代码生成**: build_runner、mobx_codegen、json_serializable、riverpod_generator、freezed
- **UI 框架**: Material Design（移动端）、Fluent UI（已弃用，请忽略）
- **国际化**: intl、flutter_localizations

## 代码风格规范

### Dart 代码风格

1. **命名规范**:
   - 类名使用 PascalCase：`UserSetting`、`AccountStore`
   - 变量和方法名使用 camelCase：`userSetting`、`fetchData`
   - 常量使用 UPPER_SNAKE_CASE：`ZOOM_QUALITY_KEY`、`IS_AMOLED_KEY`
   - 私有成员使用下划线前缀：`_UserSetting`、`_initDownloadStore`

2. **文件组织**:
   - 每个文件顶部必须包含 GPL 版权声明
   - 导入顺序：dart 库 → Flutter 库 → 第三方包 → 项目内部文件
   - 使用 `part` 指令关联生成的 `.g.dart` 文件

3. **代码格式**:
   - 使用 2 空格缩进
   - 行尾不使用分号（除非必要）
   - 遵循 Dart 官方代码风格指南

### MobX Store 规范

1. **Store 类定义模式**:
```dart
part 'store_name.g.dart';

class StoreName = _StoreNameBase with _$StoreName;

abstract class _StoreNameBase with Store {
  @observable
  Type field = defaultValue;

  @action
  void methodName() {
    // 实现
  }
}
```

2. **注解使用**:
   - 使用 `@observable` 标记可观察字段
   - 使用 `@action` 标记修改状态的方法
   - 使用 `ObservableList` 和 `ObservableStream` 处理集合和流

3. **代码生成**:
   - 所有 Store 必须包含 `part 'store_name.g.dart';`
   - 运行 `dart run build_runner build --delete-conflicting-outputs` 生成代码
   - 生成的 `.g.dart` 文件不应手动编辑

### Riverpod 使用规范

1. **Provider 定义**:
   - 使用 `@riverpod` 注解或 `StateNotifierProvider` 定义
   - 配合 `riverpod_annotation` 和代码生成使用
   - 使用 `HookConsumerWidget` 或 `ConsumerWidget` 访问 provider

2. **状态管理**:
   - 新功能优先考虑使用 Riverpod
   - 全局状态继续使用 MobX Store（位于 `lib/store/`）

### 模型类规范

1. **JSON 序列化**:
   - 使用 `json_annotation` 和 `json_serializable`
   - 包含 `fromJson` 和 `toJson` 方法
   - 运行代码生成器生成 `.g.dart` 文件

2. **不可变数据类**:
   - 使用 `freezed` 和 `freezed_annotation` 创建不可变类
   - 生成 `.freezed.dart` 文件

## 项目结构

### 目录组织

- `lib/store/` - 全局 MobX Store（用户设置、账户、下载等）
- `lib/page/` - UI 页面，按功能组织子目录
- `lib/models/` - 数据模型，带 JSON 序列化
- `lib/network/` - API 客户端和网络相关代码
- `lib/component/` - 可复用的 UI 组件
- `lib/er/` - 工具模块（hoster、fetcher、leader 等）
- `lib/custom/` - 自定义实现（缓存、日志等）
- `lib/fluent/` - Fluent UI 实现（**已弃用，不应修改**）

### 关键页面

**主框架**
- `lib/page/hello/hello_page.dart`：主框架页面，包含底部导航栏的 5 个标签页（推荐、排行、动态、搜索、设置）

**首页/推荐**
- `lib/page/hello/recom/recom_spotlight_page.dart`：推荐页（首页），展示推荐插画和 Pixivision 特辑
- `lib/page/preview/preview_page.dart`：预览页（未登录状态下的推荐内容）

**排行榜**
- `lib/page/hello/ranking/rank_page.dart`：排行榜页面，支持日榜/周榜/月榜/R18 等多种模式
- `lib/page/hello/ranking/ranking_mode/rank_mode_page.dart`：具体排行模式页面

**动态（关注）**
- `lib/page/hello/new/new_page.dart`：动态标签页，包含 4 个子标签（关注作品、收藏、画师、追更）
- `lib/page/hello/new/illust/new_illust_page.dart`：关注画师的新作品列表
- `lib/page/follow/follow_list.dart`：关注的画师列表
- `lib/page/watchlist/watchlist.dart`：追更列表

**收藏**
- `lib/page/user/bookmark/bookmark_page.dart`：收藏页面，展示用户收藏的插画
- `lib/page/user/bookmark/tag/user_bookmark_tag_page.dart`：收藏标签管理页面
- `lib/page/book/tag/book_tag_page.dart`：收藏标签选择页面

**搜索**
- `lib/page/search/search_page.dart`：搜索页面，展示热门标签和搜索入口
- `lib/page/search/result_page.dart`：搜索结果页面
- `lib/page/search/suggest/search_suggestion_page.dart`：搜索建议/自动完成页面
- `lib/page/saucenao/saucenao_page.dart`：以图搜图（SauceNAO）页面

**设置**
- `lib/page/hello/setting/setting_page.dart`：设置页面入口，包含账户、历史、下载、主题等入口
- `lib/page/hello/setting/setting_quality_page.dart`：画质设置页面, 偏好设置页面
- `lib/page/theme/theme_page.dart`：主题设置页面
- `lib/page/shield/shield_page.dart`：屏蔽设置页面（屏蔽标签/用户/插画）
- `lib/page/network/network_setting_page.dart`：网络设置页面
- `lib/page/hello/setting/save_format_page.dart`：保存格式设置页面

**用户/画师**
- `lib/page/user/users_page.dart`：用户主页（画师个人页面），包含作品、收藏、关注等标签
- `lib/page/user/works/works_page.dart`：用户作品列表页面
- `lib/page/painter/painter_list.dart`：画师列表组件

**插画详情**
- `lib/page/picture/illust_lighting_page.dart`：插画详情页面（主要使用）,纵向滑动
- `lib/page/picture/picture_list_page.dart`：插画列表页面（左右滑动浏览）
- `lib/page/picture/illust_row_page.dart`：插画横向滑动页面
- `lib/page/comment/comment_page.dart`：评论页面
- `lib/page/zoom/photo_zoom_page.dart`：图片缩放查看页面

**下载**
- `lib/page/task/job_page.dart`：下载任务管理页面，展示下载进度和历史
- `lib/page/directory/directory_page.dart`：保存目录设置页面

**浏览历史**
- `lib/page/history/history_page.dart`：插画浏览历史页面
- `lib/page/novel/history/novel_history_page.dart`：小说浏览历史页面

**小说模块**
- `lib/page/novel/novel_rail.dart`：小说模块导航入口
- `lib/page/novel/recom/novel_recom_page.dart`：小说推荐页面
- `lib/page/novel/rank/novel_rank_page.dart`：小说排行榜页面
- `lib/page/novel/bookmark/novel_bookmark_page.dart`：小说收藏页面
- `lib/page/novel/viewer/novel_store.dart`：小说阅读器

**登录/账户**
- `lib/page/login/login_page.dart`：登录页面
- `lib/page/Init/guide_page.dart`：初始引导页面（语言选择等）
- `lib/page/account/select/account_select_page.dart`：多账户切换页面

**其他**
- `lib/page/about/about_page.dart`：关于页面
- `lib/page/spotlight/spotlight_page.dart`：Pixivision 特辑页面
- `lib/page/soup/soup_page.dart`：Pixivision 文章详情页面
- `lib/page/series/illust_series_page.dart`：插画系列页面
- `lib/page/report/report_items_page.dart`：举报页面

**下载**
- `lib/page/download/downloaded_page.dart`：下载页面
- `lib/page/download/downloaded_author_page.dart`：下载作者页面

### 文件命名

- Store 文件：`*_store.dart`（如 `user_setting.dart`）
- 页面文件：`*_page.dart`（如 `hello_page.dart`）
- 模型文件：使用模型名称（如 `illust.dart`、`account.dart`）
- 组件文件：使用描述性名称（如 `illust_card.dart`、`pixiv_image.dart`）

## 开发规范

### 代码生成

在修改 Store、Model 或使用代码生成的代码后，必须运行：

```bash
dart run build_runner build --delete-conflicting-outputs
```

### 状态管理选择

- **全局状态**（用户设置、账户、下载等）→ 使用 MobX Store，放在 `lib/store/`
- **页面级状态** → 优先使用 Riverpod，也可以使用 MobX Store
- **临时状态** → 使用 `StatefulWidget` 或 `flutter_hooks`

### UI 开发

1. **Material Design**:
   - 移动端（Android/iOS）使用 Material Design
   - 使用 `MaterialApp` 和 Material 组件

2. **Fluent UI**:
   - **注意**: `lib/fluent/` 目录下的文件已不再使用，不应修改
   - `Constants.isFluent` 当前为 `false`，不应启用

3. **响应式设计**:
   - 使用 `MediaQuery` 适配不同屏幕尺寸
   - 考虑平板模式（`PAD_MODE_KEY`）

### 网络请求

- 使用 `lib/network/api_client.dart` 进行 API 调用
- 使用 `dio` 进行 HTTP 请求
- 使用 `dio_cache_interceptor` 处理缓存
- 支持中国大陆地区直连（通过 `lib/er/hoster.dart`）

### 数据持久化

- 使用 `SharedPreferences` 存储简单配置
- 使用 `sqflite` 存储结构化数据
- Windows/Linux 使用 `sqflite_common_ffi`

### 国际化

- 使用 `intl` 和 `flutter_localizations`
- 翻译文件位于 `lib/l10n/*.arb`
- 使用 `AppLocalizations` 访问翻译文本
- 在代码中使用 `I18n.of(context)` 或生成的本地化类

## 注释和文档

1. **代码注释**:
   - 使用中文注释
   - 每个类和方法都要添加注释说明
   - 复杂逻辑必须添加注释说明
   - 公共 API 应添加文档注释

2. **TODO 和 FIXME**:
   - 使用 `// TODO:` 标记待办事项
   - 使用 `// FIXME:` 标记需要修复的问题

## 错误处理

1. **异常处理**:
   - 使用 `try-catch` 捕获异常
   - 向用户显示友好的错误消息
   - 使用 `bot_toast` 显示提示

2. **空值安全**:
   - 充分利用 Dart 的空值安全特性
   - 使用 `?`、`!`、`??` 操作符处理可空类型
   - 使用 `TypeUtil` 进行类型转换和比较

## 性能优化

1. **图片加载**:
   - 使用 `cached_network_image` 缓存网络图片
   - 使用 `lib/component/pixiv_image.dart` 加载 Pixiv 图片
   - 配置图片缓存大小（默认 240MB）

2. **列表优化**:
   - 长列表使用 `ListView.builder` 或 `SliverList`
   - 使用 `waterfall_flow` 实现瀑布流布局
   - 考虑使用 `easy_refresh` 实现下拉刷新

3. **状态更新**:
   - 避免在 `build` 方法中进行耗时操作
   - 使用 `Observer` 包装需要响应 MobX 状态变化的 Widget

## 平台特定代码

1. **平台检测**:
   - 使用 `Platform.isAndroid`、`Platform.isIOS`、`Platform.isWindows`、`Platform.isLinux`
   - 使用 `kIsWeb` 检测 Web 平台（如果支持）

2. **平台特定功能**:
   - Windows/Linux: 使用 `window_manager` 管理窗口
   - Windows: 使用 `flutter_acrylic` 实现窗口特效
   - 使用 Platform Channel 调用原生功能

## 测试

- 单元测试放在 `test/` 目录
- Widget 测试使用 `flutter_test`
- 运行测试：`flutter test`

## Git 工作流

1. **提交信息**:
   - 使用中文或英文描述提交内容
   - 格式：`类型: 简短描述`

2. **分支管理**:
   - `main` 分支用于稳定版本
   - 功能开发使用功能分支

## 注意事项

1. **不要修改**:
   - `lib/fluent/` 目录下的文件（已弃用）
   - 生成的 `.g.dart`、`.freezed.dart` 文件
   - `build/` 目录下的构建产物

2. **必须运行代码生成**:
   - 修改 Store、Model 或使用代码生成注解后
   - 提交代码前确保所有生成文件是最新的

3. **依赖管理**:
   - 添加新依赖后运行 `flutter pub get`
   - 检查 `pubspec.lock` 的变更

4. **构建命令**:
   - Android: `flutter build apk` 或 `flutter build appbundle`
   - iOS: `flutter build ios`
   - Windows: `flutter build windows` + `dart run msix:create`

## 代码审查检查清单

- [ ] 代码生成文件已更新（`.g.dart`、`.freezed.dart`）
- [ ] 遵循命名规范
- [ ] 添加了必要的注释
- [ ] 处理了空值和异常
- [ ] 没有修改已弃用的 Fluent UI 代码
- [ ] 国际化文本已添加
- [ ] 性能考虑（列表优化、图片缓存等）
- [ ] 平台兼容性检查

## 常用命令

```bash
# 获取依赖
flutter pub get

# 运行代码生成
dart run build_runner build --delete-conflicting-outputs

# 运行应用
flutter run

# 构建 Android APK
flutter build apk

# 构建 Windows MSIX
flutter build windows
dart run msix:create

# 清理构建
flutter clean
```

## 参考文档

- [Flutter 官方文档](https://flutter.dev/docs)
- [MobX.dart 文档](https://mobx.netlify.app/)
- [Riverpod 文档](https://riverpod.dev/)
- [Freezed 文档](https://pub.dev/packages/freezed)
- 项目 README.md 和 CLAUDE.md

